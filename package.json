{
  "name": "inside-nature",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env server.mjs",
    "build": "tsc --noEmit && vite build",
    "check": "tsc --noEmit",
    "preview": "node --env-file=.env server.mjs"
  },
  "dependencies": {
    "three": "^0.159.0"
  },
  "devDependencies": {
    "@types/three": "^0.159.0",
    "typescript": "^5.7.2",
    "vite": "^6.4.3"
  }
}

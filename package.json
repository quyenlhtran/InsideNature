{
  "name": "inside-nature",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --watch-path=server --watch-path=server.mjs --watch-path=app.config.mjs --env-file=.env server.mjs",
    "build": "tsc --noEmit && vite build",
    "check": "tsc --noEmit",
    "test": "node --test server/ai/scene.test.mjs",
    "preview": "node --env-file=.env server.mjs",
    "generate-audio": "node --env-file=.env scripts/generate-audio.mjs"
  },
  "dependencies": {
    "three": "^0.159.0"
  },
  "devDependencies": {
    "@types/three": "^0.159.0",
    "playwright-core": "^1.63.0",
    "typescript": "^5.7.2",
    "vite": "^6.4.3"
  }
}
